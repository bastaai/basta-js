query GET_ACCOUNT_BY_ID($accountId: String!) {
  account(id: $accountId) {
    __typename
    id
    name
    handle
    description
    imageUrl
    links {
      __typename
      type
      url
    }
  }
}

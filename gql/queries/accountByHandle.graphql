query GET_ACCOUNT_BY_HANDLE($handle: String!) {
  accountByHandle(handle: $handle) {
    id
    name
    handle
    description
    imageUrl
    links {
      type
      url
      __typename
    }
    __typename
  }
}
